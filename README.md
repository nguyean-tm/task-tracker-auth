# task-tracker-auth

JWT authentication for a task tracker app.

**Stack**: React + Vite (frontend) · FastAPI + SQLAlchemy + Alembic + PostgreSQL (backend) · python-jose JWT · bcrypt

See `backend/README.md` for backend setup instructions.
